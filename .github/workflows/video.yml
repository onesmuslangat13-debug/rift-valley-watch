      - name: Generate V2 MP4
        run: |
          mkdir -p output
          echo "=========================================="
          echo "STARTING VIDEO GENERATOR"
          echo "=========================================="
          python -u scripts/video_generator.py 2>&1 | tee video_generator.log
          STATUS=${PIPESTATUS[0]}
          echo "=========================================="
          echo "VIDEO GENERATOR EXIT CODE: $STATUS"
          echo "=========================================="
          exit $STATUS
